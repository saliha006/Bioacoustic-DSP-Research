# --- Audio recordings & intermediate chunks (far too large for GitHub) ---
*.WAV
*.wav
recordings/
**/Extracted chunks/
**/chunks/
birdnet_work*/

# --- Python virtual environments (huge, machine-specific) ---
.venv*/
**/.venv*/
__pycache__/
*.pyc

# --- BirdNET model checkpoints (downloaded, ~224 MB) ---
**/checkpoints/

# --- Intermediate per-chunk BirdNET outputs (regenerable working dirs) ---
**/birdnet_results/
**/birdnet_results_site5/
**/birdnet_results_latlon/
**/birdnet_results_1hr/

# --- Backups & logs ---
**/_pre-recombine-backup/
*.log

# --- Site photos (not for public repo) ---
sites/

# --- Reference/QA clips, not for public repo ---
reference-examples/

# --- Processing pipeline code: professor-provided, IP status unconfirmed.
# Do not push until cleared — ask before end of website build.
processing/

# --- Local planning/context docs (Claude/Codex instructions, notes) — never pushed ---
*.md
!README.md

# --- OS cruft ---
Thumbs.db
.DS_Store

# --- Local dev tooling (Impeccable design skill + its puppeteer dep) — not project code ---
.claude/
/node_modules/

# Locally-installed design skill packs (not project code)
.agents/
skills-lock.json
